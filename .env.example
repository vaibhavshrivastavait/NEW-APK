# MHT Assessment - Environment Configuration Example
# Copy this file to .env and update values as needed

# Expo Development Configuration
EXPO_PACKAGER_HOSTNAME=localhost
EXPO_USE_FAST_RESOLVER=1

# Backend API Configuration (for development only)
EXPO_PUBLIC_BACKEND_URL=http://localhost:8001

# Note: This app works completely offline
# Backend URL is only used during development and testing
# The production APK includes all data and works without internet connection